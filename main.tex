\documentclass[aspectratio=169]{beamer}

% Theme and color settings
% \usetheme{Madrid}
% \usecolortheme{default}
\usetheme{Pittsburgh}
\usecolortheme{beaver}

% Packages
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{graphicx}
\usepackage{bm}
\usepackage{tikz}
\usepackage{annotate-equations}
\usepackage[backend=bibtex, style=authoryear, natbib=true, maxcitenames=2]{biblatex}
\addbibresource{references.bib}

\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}[frame number]
\setbeamertemplate{frametitle}[default][left]
\renewcommand{\emph}[1]{\textit{\textcolor{blue}{#1}}}
\newcommand{\remph}[1]{\textit{\textcolor{red}{#1}}}

% Title information
\title[Influence Functions]{Understanding Influence Functions: \\ From Edge Edits in Graphs to Group Influence in LLMs}
\author{Dongwoo Kim}
\institute[ANU \& POSTECH]{
    Visiting Fellow, School of Computing, Australian National University \\
    Associate Professor, POSTECH
}
\date{April 2026}

\begin{document}

% Slide 1: Title
\begin{frame}
    \titlepage
    \begin{tikzpicture}[remember picture, overlay]
        \node[anchor=south east, inner sep=10pt] at (current page.south east) {
            \includegraphics[height=1.8cm]{figures/POSTECH_emblem.pdf}
        };
    \end{tikzpicture}
\end{frame}

\begin{frame}{Part 1: The Theory of Influence Functions} 
    \vfill
    \begin{center}
        
        \bigskip
        
        \Large\textbf{``Can we quantify the \remph{influence} of a single data point\thinspace?''}

        \pause
        \bigskip

        \large\textit{But influence on \emph{what}\thinspace?}
    \end{center}
    \vfill
\end{frame}

% Application slides
\begin{frame}{Application: Model Interpretability}
    \begin{center}
        \includegraphics[height=0.65\textheight]{figures/app_interpretability.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Which training examples were most helpful or harmful for \emph{a prediction}?\\
        \textit{--- \citet{koh2017understanding}}}
    \end{center}
\end{frame}
% Notes: Figure 4 from Koh & Liang (2017). Left: Inception vs RBF SVM scatter plot of
% influence scores vs Euclidean distance. Green dots = fish, red dots = dogs.
% Bottom right: The two most helpful training images for each model on the test image (a dog).
% Top right: An image of a dog in the training set that helped the Inception model correctly
% classify the test image as a fish. Key point: Inception picks up on pattern-matched features
% (clownfish patterns), while RBF SVM relies on raw pixel distance.

\begin{frame}{Application: Data Poisoning Attacks}
    \begin{center}
        \includegraphics[height=0.55\textheight]{figures/app_poisoning.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small A small, visually-indistinguishable perturbation to a single training image\\
        can flip the model's prediction on \emph{multiple test images}.\\
        \textit{--- \citet{koh2017understanding}}}
    \end{center}
\end{frame}
% Notes: Figure 5 from Koh & Liang (2017). Training-set attack demonstration.
% A small perturbation (+epsilon) is added to a single training image, making it
% visually indistinguishable from the original. This perturbation is crafted using
% influence functions to maximize the average loss over a set of 30 test images.
% Result: the attack flipped predictions on 16 of 30 test images (57%).
% Key point: influence functions reveal which training points are most vulnerable,
% enabling both stronger attacks and better defenses.
\begin{frame}{Application: Data Valuation}
    \begin{center}
        \includegraphics[height=0.55\textheight]{figures/data_valuation.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Assigning a value to each training example based on its \emph{contribution} to the model's performance.\\
        \textit{--- \citet{jia2019towards}}}
    \end{center}
\end{frame}

\begin{frame}{Application: Fixing Mislabeled Data}
    \begin{center}
        \includegraphics[height=0.55\textheight]{figures/app_mislabeled.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Using influence to prioritize which labels to check outperforms\\
        random inspection and loss-based selection.\\
        \textit{--- \citet{koh2017understanding} } }
    \end{center}
\end{frame}
% Notes: Figure 6 from Koh & Liang (2017). Fixing mislabeled examples on the Enron spam dataset.
% Left plot: test accuracy vs fraction of training data checked. Influence-based selection (blue)
% finds mislabeled points much faster than random (red) or loss-based (green) selection.
% Right plot: fraction of flipped labels detected vs fraction checked — same trend.
% 10% of training labels were randomly flipped. Using influence to prioritize which points
% to inspect allows repairing the dataset without checking too many points.

\begin{frame}{Application: Understanding LLM Generalization}
    \begin{center}
        \includegraphics[height=0.7\textheight]{figures/app_llm_influence.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Tracing an LLM's response back to its most influential training sequences.\\
        \textit{--- \citet{grosse2023studying} (Anthropic)}}
    \end{center}
\end{frame}
% Notes: Figure from Grosse et al. (2023), Anthropic. Query: "shutdown" — the model is asked
% whether it consents to being shut down. The completion expresses a desire to continue existing.
% The top influential training sequences for a 52B parameter model are shown with color-coded
% token highlighting (green = high positive influence, red = high negative influence).
% Key finding: at large scale, influential sequences are thematically related (survival instinct,
% desire to continue living) rather than sharing surface-level token overlap.
% This demonstrates that LLMs generalize abstractly from training data, not via memorization.

% Slide 2: Motivation
\begin{frame}{From Intuition to Formalization}
    \textbf{Core Question:} How does \remph{a single training data point} affect our model's predictions?
    
    \vspace{0.5cm}
    Imagine a scenario where we want to understand a model's behavior, find mislabeled data, or defend against data poisoning attacks.
    
    \pause
    \vspace{0.5cm}
    \textbf{The Naive Approach (\emph{Leave-One-Out} (LOO) Retraining):}
    \begin{itemize}
        \item Remove the target data point $z$ from the training set.
        \item Retrain the model entirely from scratch.
        \item Compare the new parameters or predictions with the original ones.
    \end{itemize}
    
    \pause
    \vspace{0.5cm}
    \textbf{The Problem:} %Retraining a modern neural network for every single training point is 
    LOO is \emph{computationally impossible}. We need an efficient approximation.
\end{frame}

% Slide 3: Empirical Risk Minimization
\begin{frame}{Mathematical Setup}
    Let $z = (x, y)$ be a training point, $L(z, \theta)$ the loss, and $\hat{\theta}$ the ERM solution:

    \begin{equation}
        \hat{\theta} = \arg\min_{\theta} \frac{1}{n} \sum_{i=1}^{n} L(z_i, \theta) = \arg\min_{\theta} R(\theta)
    \end{equation}

    \pause

    \vspace{0.5cm}
    Upweight a single training point $z$ by $\epsilon$:

    \begin{equation}
        \hat{\theta}_{\epsilon, z} = \arg\min_{\theta} \eqnmarkbox[blue]{erm}{R(\theta)} + \eqnmarkbox[red]{upw}{\epsilon \, L(z, \theta)}
    \end{equation}
    \annotate[yshift=-1em]{below,left}{erm}{\small empirical risk}
    \annotate[yshift=-1em]{below,right}{upw}{\small extra weight $\epsilon$ on $z$}

\end{frame}

% Slide 4a: Deriving the Influence Function (Part 1)
\begin{frame}{Deriving the Influence Function (I)}
    \textbf{Goal:} compute how $\hat{\theta}_{\epsilon, z}$ changes as we vary $\epsilon$, i.e.\ $\left.\dfrac{d\hat{\theta}_{\epsilon,z}}{d\epsilon}\right|_{\epsilon=0}$.

    \vspace{0.5cm}
    \textbf{Step 1.} Stationarity condition --- $\hat{\theta}_{\epsilon, z}$ minimizes the upweighted objective:
    \begin{equation}
        \nabla_\theta R(\hat{\theta}_{\epsilon,z}) + \epsilon\,\nabla_\theta L(z, \hat{\theta}_{\epsilon,z}) = 0
    \end{equation}

    \pause
    \vspace{0.3cm}
    \textbf{Step 2.} Taylor expansion of $\nabla_\theta R(\hat{\theta}_{\epsilon,z})$ around $\hat{\theta}$ (at $\epsilon=0$):
    \[
        \nabla_\theta R(\hat{\theta}_{\epsilon,z}) \approx \underbrace{\nabla_\theta R(\hat{\theta})}_{=\,0} + \nabla_\theta^2 R(\hat{\theta})(\hat{\theta}_{\epsilon,z} - \hat{\theta})
    \]
\end{frame}

% Slide 4b: Deriving the Influence Function (Part 2)
\begin{frame}{Deriving the Influence Function (II)}
    \textbf{Step 3.} Substitute into the stationarity condition:
    \[
        \underbrace{\nabla_\theta^2 R(\hat{\theta})}_{H_{\hat{\theta}}} (\hat{\theta}_{\epsilon,z} - \hat{\theta}) + \epsilon\,\nabla_\theta L(z, \hat{\theta}) = 0
    \]

    \pause
    
    Rearranging:
    \[
        \eqnmarkbox[orange]{paramchange}{\hat{\theta}_{\epsilon,z} - \hat{\theta}} = -\epsilon\, \eqnmarkbox[blue]{invH}{H_{\hat{\theta}}^{-1}} \eqnmarkbox[red]{grad}{\nabla_\theta L(z, \hat{\theta})}
    \]
    \annotate[yshift=1em]{above}{invH}{\small curvature of the loss surface}
    \annotate[yshift=-1em]{below,left}{paramchange}{\small change in parameters}
    \annotate[yshift=-1em]{below,right}{grad}{\small direction of steepest ascent for $z$}

    \pause
    \vspace{0.5cm}
    \textbf{Step 4.} Formal definition of the \emph{influence function} is:
    \[
        \mathcal{I}_\theta(z) \;:=\; \left.\frac{d\hat{\theta}_{\epsilon,z}}{d\epsilon}\right|_{\epsilon=0} = -H_{\hat{\theta}}^{-1}\,\nabla_\theta L(z, \hat{\theta})
    \]
\end{frame}

\begin{frame}{Approximating Leave-One-Out}
    Removing $z$ $\Leftrightarrow$ setting $\epsilon = -\frac{1}{n}$.

    \vspace{0.3cm}
    \textbf{First-order approximation} using the influence function $\mathcal{I}_\theta(z)$:
    \begin{equation}
        \hat{\theta}_{-z} - \hat{\theta} \approx -\tfrac{1}{n}\,\mathcal{I}_\theta(z) = \frac{1}{n} H_{\hat{\theta}}^{-1} \nabla_{\theta} L(z, \hat{\theta})
    \end{equation}

    \pause
    \vspace{0.3cm}
    Closed-form LOO approximation:
    \begin{equation}
        \hat{\theta}_{-z} \approx \hat{\theta} - \tfrac{1}{n}\,\mathcal{I}_\theta(z) = \hat{\theta} + \frac{1}{n} H_{\hat{\theta}}^{-1} \nabla_{\theta} L(z, \hat{\theta})
    \end{equation}
\end{frame}


% Slide 5: Influence on Test Loss
\begin{frame}{Influence on Test Predictions}
    How does removing $z$ affect the loss on a test point $z_{\text{test}}$?

    \pause
    
    \vspace{0.3cm}
    \textbf{Chain rule} \citep{koh2017understanding}: remove point $\to$ change in parameters $\to$ change in prediction.
    \vspace{0.5cm}
    \begin{align*}
        \mathcal{I}_L(z, z_{\text{test}}) \;\;=\;\;
        \eqnmarkbox[purple]{prediff}{\frac{d\, L(z_{\text{test}}, \hat{\theta}_{\epsilon,z})}{d\epsilon}}
        &= \eqnmarkbox[orange]{testgrad}{\nabla_\theta L(z_{\text{test}}, \hat{\theta})^\top}
        \cdot
        \eqnmarkbox[teal]{paramdiff}{\frac{d\hat{\theta}_{\epsilon,z}}{d\epsilon}} \\[2.5em]
        &= -\, \nabla_{\theta} L(z_{\text{test}}, \hat{\theta})^\top\, H_{\hat{\theta}}^{-1}\, \nabla_{\theta} L(z, \hat{\theta})
    \end{align*}
    \annotate[yshift=0.8em]{above,left}{prediff}{\small change in prediction}
    \annotate[yshift=1.3em]{above}{testgrad}{\small test gradient}
    \annotate[yshift=-0.5em]{below,left}{paramdiff}{\small $\mathcal{I}_\theta(z)$: change in parameters}
\end{frame}

\begin{frame}{Generalizing to Any Evaluation Function}
    The test loss $L(z_{\text{test}}, \theta)$ is just one choice.
    We can replace it with \emph{any differentiable evaluation function} $f(\theta)$:

    \begin{equation}
        \mathcal{I}_{f}(z) = - \textcolor{red}{\nabla_{\theta} f(\hat{\theta})^\top} H_{\hat{\theta}}^{-1} \nabla_{\theta} L(z, \hat{\theta})
    \end{equation}

    \pause
    \vspace{0.3cm}
    \textbf{Examples:}
    \begin{itemize}\setlength\itemsep{0.6em}
        \item $f(\theta) = L(z_{\text{test}}, \theta)$: influence on a single test loss (previous slide)
        \item $f(\theta) = \frac{1}{|\mathcal{D}_{\text{val}}|}\sum_{z' \in \mathcal{D}_{\text{val}}} L(z', \theta)$: influence on validation loss
        \item $f(\theta) = L_{\text{safety}}(\mathcal{D}_{\text{safe}}, \theta)$: influence on safety alignment
        \item $f(\theta) = \|\theta\|^2$: influence on model complexity
    \end{itemize}
\end{frame}

\begin{frame}{Influence vs.\ Leave-One-Out Retraining}
    \begin{center}
        \includegraphics[height=0.7\textheight]{figures/correlation1.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Correlation between influence function estimates and actual LOO retraining effects (Logistic Regression).}
    \end{center}
\end{frame}

\begin{frame}{However ... }
    \begin{center}
        \includegraphics[height=0.6\textheight]{figures/correlation2.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small The newly added figure with CNN does not show strong correlation.}
    \end{center}
\end{frame}


\begin{frame}{Fix 1: Damping --- Addressing Non-Convexity and Non-Convergence}
    \citet{koh2017understanding}: form a \textbf{convex quadratic approximation} around the current parameters $\tilde{\theta}$ (not necessarily a minimum):

    \begin{equation}
        \tilde{L}(z, \theta) = \eqnmarkbox[gray]{qconst}{L(z, \tilde{\theta})} + \eqnmarkbox[orange]{qlinear}{\nabla L(z, \tilde{\theta})^\top (\theta - \tilde{\theta})} + \frac{1}{2}(\theta - \tilde{\theta})^\top \eqnmarkbox[violet]{qdamp}{(H_{\tilde{\theta}} + \lambda I)} (\theta - \tilde{\theta})
    \end{equation}
    \annotate[yshift=-1em]{below,left}{qconst}{\small constant}
    \annotate[yshift=-1em]{below}{qlinear}{\small linear term (gradient)}
    \annotate[yshift=-2.5em]{below,right}{qdamp}{\small damped Hessian}

    \vspace{1.3cm}
    \begin{itemize}\setlength\itemsep{0.4em}
        \item \textbf{Non-convexity fix:} damping \remph{$\lambda I$} forces $(H_{\tilde{\theta}} + \lambda I) \succ 0$ --- a convex bowl.
        \item \textbf{Non-convergence fix:} expanding around \emph{any} $\tilde{\theta}$ (not the minimum) sidesteps the assumption $\nabla R(\hat\theta) = 0$.
    \end{itemize}
\end{frame}


\begin{frame}{Fix 2: Gauss-Newton Hessian --- Addressing Efficiency}
    The damped Hessian is still expensive: HVPs need second-order autograd.

    \vspace{0.5cm}
    \textbf{Gauss-Newton Hessian (GNH):} a PSD approximation built from first-order quantities.
    \vspace{1em}
    \begin{equation}
        G = \eqnmarkbox[red]{jac1}{J_{y\theta}^\top} \eqnmarkbox[blue]{lossH}{H_y} \eqnmarkbox[red]{jac2}{J_{y\theta}}
    \end{equation}
    \annotate[yshift=-1em]{below,left}{jac2}{\small Jacobian of network outputs w.r.t.\ parameters}
    \annotate[yshift=1em]{above}{lossH}{\small Hessian of loss w.r.t.\ outputs (e.g., logits)}

    \vspace{1em}
    \emph{Why it's the practical choice:}
    \begin{itemize}\setlength\itemsep{0.3em}
        \item HVPs use only \remph{first-order} autograd (two backprops) --- cheaper and lower memory than second-order.
        \item $H_y$ is tiny ($c\times c$ for $c$ outputs, often diagonal for softmax + cross-entropy).
    \end{itemize}
\end{frame}


\begin{frame}{So, do Influence Functions Work in Practice?}
    \begin{center}
        \includegraphics[width=\linewidth]{figures/fragile.pdf}
    \end{center}
\end{frame}

\begin{frame}{So, do Influence Functions Work in Practice?}
    \begin{center}
        \includegraphics[width=\linewidth]{figures/fragile_exp.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small It is working time to time...\\
        \citet{basu2021influence}}
    \end{center}
\end{frame}


% % Slide 7: Challenges in Modern Deep Learning
% \begin{frame}{Challenges in Modern Architectures}
%     While elegant, applying traditional influence functions to deep neural networks presents significant hurdles:
    
%     \vspace{0.3cm}
%     \begin{itemize}
%         \item \textbf{Computational Cost:} Storing and inverting the exact Hessian matrix requires \emph{$O(d^3)$} operations, which is infeasible for millions of parameters \citep{koh2017understanding, agarwal2017second}.
%         \pause
%         \item \textbf{Non Convexity:} Neural networks have non convex loss landscapes. The Hessian is often \emph{singular or contains negative eigenvalues}, violating the strict convexity assumption \citep{basu2021influence}.
%         \pause
%         \item \textbf{Non-Convergence:} We rarely train neural networks to a true local minimum (e.g., we use early stopping). Therefore, the assumption that the gradient \emph{$\nabla_{\theta} L(\hat{\theta}) = 0$ is violated.}
%         \pause
%         \item \textbf{Structural Complexity:} Standard influence functions assume independent, identically distributed data points. 
%     \end{itemize}

% \end{frame}


% Slide 10: The Real Question being Answered
\begin{frame}{What are Influence Functions Actually Computing?}
    Influence function is not entirely wrong!

    \vspace{0.5cm}

    Then, in which regime, does influence function work correctly?

    \vspace{0.5cm}
    
    $\Rightarrow$ We \remph{do not} retrain the model from scratch when we compute influence functions. 
    
    Instead, we solve a \textbf{proximal optimization problem} that penalizes deviation from the current parameters and predictions.
    
\end{frame}


\begin{frame}{The PBRF: Formal Definition \citep{bae2022if}}
    Given current parameters $\tilde{\theta}$, the PBRF response to upweighting $z$ by $\epsilon$ is:
    \vspace{.3cm}
    \[
        \theta^{\text{PBRF}}_{\epsilon, z}
        \;:=\;
        \arg\min_{\theta}\;
        \eqnmarkbox[blue]{breg}{\frac{1}{n}\sum_{i=1}^{n} D_{L^{(i)}}\!\bigl(f(\theta, x_i),\, f(\tilde{\theta}, x_i)\bigr)}
        \;+\;
        \eqnmarkbox[red]{upw}{\epsilon\,L(z, \theta)}
        \;+\;
        \eqnmarkbox[violet]{prox}{\tfrac{\lambda}{2}\|\theta - \tilde{\theta}\|^2}
    \]
    \annotate[yshift=-0.5em]{below,left}{breg}{\small \shortstack[l]{Bregman divergence \\ preserves predictions on other data}}
    \annotate[yshift=-0.5em]{below}{upw}{\small upweight $z$}
    \annotate[yshift=0.5em]{above,left}{prox}{\small proximity to $\tilde{\theta}$}

    \vspace{1.2cm}
    \pause
    \textbf{Key properties:}
    \begin{itemize}\setlength\itemsep{0.3em}
        \item Objective is \remph{strictly convex} in $\theta$ (for $\lambda > 0$).
        \item No assumption that $\tilde{\theta}$ is a stationary point.
        \item Taylor-expanding gives the same influence formula (GNH+damping).
    \end{itemize}
\end{frame}

\begin{frame}{So, do Influence Functions Work in Practice?}
    \begin{center}
        \includegraphics[width=\linewidth]{figures/pbrf.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small PBRF with GNH and damping shows strong correlation with LOO retraining effects. \citep{bae2022if}\\}
    \end{center}
\end{frame}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Part 2: Influence Functions for Edge Edits in GNNs
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}{Part 2: Influence of Structured Data}
    \vfill
    \begin{center}
        \Large\textbf{``Influence beyond i.i.d.: when removing one affects many''}

        % \vspace{0.5cm}
        % \normalsize
        % \citet{heo2025influence}\\
        % \textit{NeurIPS 2025}
    \end{center}
    \vfill
\end{frame}

\begin{frame}{The i.i.d.\ Assumption Breaks Down}
    Part 1 assumed training data are \emph{independent}:

    \vspace{0.3cm}
    \begin{center}
        Remove $z_i$ $\Rightarrow$ only $z_i$'s contribution changes.
    \end{center}

    \pause
    \vspace{0.5cm}
    In many real-world settings, data points are \remph{interconnected}:

    \vspace{0.3cm}
    \begin{itemize}
        \item \textbf{Graphs:} nodes share information via edges (social networks, molecules, citation graphs)
        \item \textbf{Sequences:} tokens depend on context (language models)
        \item \textbf{Groups:} correlated subsets of data (demographic slices, data sources)
    \end{itemize}

    \pause
    \vspace{0.5cm}
    Modifying one element can \remph{cascade} through the structure, affecting many predictions simultaneously.
\end{frame}

\begin{frame}{GNNs: Message Passing on Graphs}
    \begin{columns}[c]
    \begin{column}{0.55\textwidth}
    \begin{center}
    \begin{tikzpicture}[
        scale=0.85,
        node/.style={circle, draw, thick, minimum size=8mm, font=\small},
        msg/.style={->, thick, >=stealth, shorten >=2pt, shorten <=2pt},
    ]
        % Nodes
        \node[node, fill=red!20] (v) at (3, 2) {$v$};
        \node[node, fill=blue!20] (a) at (1, 3.5) {$a$};
        \node[node, fill=blue!20] (b) at (1, 0.5) {$b$};
        \node[node, fill=blue!20] (c) at (5, 3.5) {$c$};
        \node[node, fill=gray!20] (d) at (5, 0.5) {$d$};

        % Edges
        \draw[thick] (v) -- (a);
        \draw[thick] (v) -- (b);
        \draw[thick] (v) -- (c);
        \draw[thick, red, dashed, line width=1.5pt] (v) -- (d);
        \draw[thick] (a) -- (b);
        \draw[thick] (c) -- (d);

        % Messages
        \draw[msg, blue!70] (a) to[bend right=15] node[left, font=\scriptsize] {$m_a$} (v);
        \draw[msg, blue!70] (b) to[bend left=15] node[left, font=\scriptsize] {$m_b$} (v);
        \draw[msg, blue!70] (c) to[bend left=15] node[right, font=\scriptsize] {$m_c$} (v);
        \draw[msg, red!50, dashed] (d) to[bend right=15] node[right, font=\scriptsize] {$m_d$?} (v);

        % Label
        \node[below, font=\scriptsize\itshape, red!70!black] at (4, 0.1) {remove this edge?};
    \end{tikzpicture}
    \end{center}
    \end{column}
    \begin{column}{0.45\textwidth}
        Each node updates its embedding by \emph{aggregating messages} from neighbors:

        \vspace{0.3cm}
        $h_v^{(l+1)} = \sigma\!\left(\sum_{u \in \mathcal{N}(v)} W^{(l)} h_u^{(l)}\right)$

        \vspace{0.5cm}
        \textbf{Key insight:} removing edge $(v, d)$ changes:
        \begin{itemize}
            \item What $v$ ``sees'' (message)
            \item How $\theta$ is learned (parameter)
        \end{itemize}
    \end{column}
    \end{columns}
\end{frame}

\begin{frame}{What Can We Perturb in Structured Data?}
    Beyond removing a single data point $z$:

    \vspace{0.5cm}
    \begin{itemize}
        \item \textbf{Edge edits:} add or remove a connection $e = (u,v)$ in a graph
        \item \textbf{Node removal:} remove a node and all its edges
        \item \textbf{Group removal:} remove a correlated subset of data
        \item \textbf{Feature perturbation:} modify attributes that propagate through structure
    \end{itemize}

    \pause
    \vspace{0.5cm}
    We start with \textbf{edge edits in GNNs} --- the simplest structural perturbation, but one that the standard influence function framework cannot handle.

    \vspace{0.3cm}
    \hfill \citet{heo2025influence}, \textit{NeurIPS 2025}
\end{frame}

\begin{frame}{Why Edge Influence Is Hard}
    An edge edit affects GNN predictions through \emph{two distinct mechanisms}:

    \vspace{0.5cm}
    \begin{columns}[c]
    \begin{column}{0.48\textwidth}
        \centering
        \textbf{1. Parameter Shift}\\
        \vspace{0.3cm}
        Retraining on the modified graph changes the optimal parameters $\theta$.

        \vspace{0.3cm}
        $\theta^* \to \theta^*_{-e}$

        \vspace{0.3cm}
        {\small (Same as standard IF)}
    \end{column}
    \pause
    \begin{column}{0.04\textwidth}
        \centering
        \Large $+$
    \end{column}
    \begin{column}{0.48\textwidth}
        \centering
        \textbf{2. Message Propagation}\\
        \vspace{0.3cm}
        Removing/adding an edge changes how information flows through the network.

        \vspace{0.3cm}
        $G \to G \setminus e$

        \vspace{0.3cm}
        {\small (Unique to graph data)}
    \end{column}
    \end{columns}

    \pause
    \vspace{0.5cm}
    \centering
    Prior work (GIF) only captures the first pathway $\Rightarrow$ poor correlation ($\sim$0.1).
\end{frame}

\begin{frame}{Existing Approach: GIF}
    Graph Influence Function (GIF) adapts standard IF to graphs, but assumes \remph{convexity}:

    \vspace{0.3cm}
    \begin{itemize}
        \item Treats edge removal as a data perturbation
        \item Ignores the \remph{message propagation effect} of topology changes
        \item Assumes convex loss $\Rightarrow$ invalid for GNNs
    \end{itemize}

    \pause
    \vspace{0.5cm}
    \textbf{Result:} Correlation between predicted and actual influence $\approx$ 0.09--0.14.

    \vspace{0.5cm}
    We need a formulation that:
    \begin{enumerate}
        \item Handles \emph{non-convex} GNN losses
        \item Captures \emph{both} parameter shift and message propagation
    \end{enumerate}
\end{frame}

\begin{frame}{Our Approach: Edge-Edit PBRF}
    We extend the \textbf{Proximal Bregman Response Function} to edge edits in GNNs:

    \vspace{0.3cm}
    \begin{equation}
        \theta^*_\epsilon := \arg\min_{\theta} \frac{1}{N} \sum_v \eqnmarkbox[blue]{bregman}{D_L(h^G_v, h^{G,\theta_s}_v)} + \eqnmarkbox[red]{prox}{\frac{\lambda}{2}\|\theta - \theta_s\|^2} + \epsilon \sum_v \eqnmarkbox[orange]{edgeeff}{\left(L(h^G_v) - L(h^{G_{-e}}_v)\right)}
    \end{equation}
    \annotate[yshift=-0.5em]{below,left}{bregman}{\small Bregman divergence}
    \annotate[yshift=-1.5em]{below}{prox}{\small proximity to current params}
    \annotate[yshift=-0.5em]{below,right}{edgeeff}{\small effect of edge edit}
\end{frame}

\begin{frame}{Influence Decomposition}
    The total influence of an edge $e$ on a prediction $f$ decomposes as:

    \vspace{0.5cm}
    \begin{equation}
        \underbrace{\mathcal{I}(e, f)}_{\text{total influence}} = \underbrace{\mathcal{I}_{\text{param}}(e, f)}_{\text{parameter shift}} + \underbrace{\mathcal{I}_{\text{msg}}(e, f)}_{\text{message propagation}}
    \end{equation}

    \pause
    \vspace{0.5cm}
    \textbf{Parameter shift} (requires inverse Hessian):
    \begin{equation}
        \mathcal{I}_{\text{param}}(e, f) = \frac{1}{N} \nabla_\theta f^\top \, G^{-1} \sum_v \left(\nabla_\theta L(h^G_v) - \nabla_\theta L(h^{G_{-e}}_v)\right)
    \end{equation}

    \pause
    \vspace{0.3cm}
    \textbf{Message propagation} (direct topology effect):
    \begin{equation}
        \mathcal{I}_{\text{msg}}(e, f) = (2 \cdot \mathbf{1}[e \in E] - 1)\left(\frac{\partial f}{\partial A_{uv}} + \frac{\partial f}{\partial A_{vu}}\right)
    \end{equation}
\end{frame}

\begin{frame}{Generalized Gauss-Newton Hessian}
    For non-convex GNNs, we approximate the Hessian using:

    \vspace{1.2cm}
    \begin{equation}
        G = \eqnmarkbox[red]{gjac1}{J_{h\theta}^\top} \eqnmarkbox[blue]{gHL}{H_h} \eqnmarkbox[red]{gjac2}{J_{h\theta}} + \eqnmarkbox[violet]{glambda}{\lambda I}
    \end{equation}
    \annotate[yshift=-1em]{below,left}{gjac2}{\small Jacobian of node embeddings w.r.t.\ $\theta$}
    \annotate[yshift=2.2em]{above}{gHL}{\small Hessian of loss w.r.t.\ embeddings}
    \annotate[yshift=1em]{above,right}{glambda}{\small damping}

    \vspace{0.8cm}
    \begin{itemize}
        \item Guaranteed positive semi-definite $\Rightarrow$ no convexity needed
        \item Inverse approximated via \textbf{LiSSA} for scalability
    \end{itemize}
\end{frame}

\begin{frame}{Results: Influence Prediction Accuracy}
    \begin{center}
        \includegraphics[width=0.8\linewidth]{figures/edge_influence.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small Both components are essential --- consistent across GCN, GAT, ChebNet.\\
        \citet{heo2025influence}}
    \end{center}
\end{frame}

\begin{frame}{Edge Influence on a Barbell Graph}
    \begin{center}
        \includegraphics[width=0.85\linewidth]{figures/gnn_edge_influence.pdf}
    \end{center}
    \vspace{-0.3cm}
    \begin{center}
        {\small \textcolor{blue}{Blue}: beneficial edges, \textcolor{red}{Red}: harmful edges.\\
        Edges connecting clusters mitigate over-squashing but amplify over-smoothing.\\
        \citet{heo2025influence}}
    \end{center}
\end{frame}

\begin{frame}{Applications}
    \textbf{1. Adversarial Attack Detection}
    \begin{itemize}
        \item Identifies edges most harmful to model accuracy
        \item Outperforms DICE and PRBCD baselines
    \end{itemize}

    \vspace{0.3cm}
    \textbf{2. Graph Rewiring Analysis}
    \begin{itemize}
        \item Reveals unintended side effects of existing rewiring strategies (e.g., BORF)
        \item Quantifies trade-offs of topology modifications
    \end{itemize}

    \vspace{0.3cm}
    \textbf{3. Over-squashing \& Over-smoothing}
    \begin{itemize}
        \item Predicts which edges contribute to information bottlenecks
        \item Measures impact on Dirichlet energy
    \end{itemize}
\end{frame}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Part 3: Second-Order Group Influence for LLMs
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{frame}{Part 3: Group Influence and Data Interactions}
    \vfill
    \begin{center}
        \Large\textbf{``Which data complement each other, and which interfere?''}

        \vspace{0.5cm}
        \normalsize
        Work in progress
    \end{center}
    \vfill
\end{frame}

\begin{frame}{Limitation of First-Order Influence}
    Let $g_f = \nabla_\theta f(\theta^*)$, $g_S = \nabla_\theta \mathcal{L}_S(\theta^*)$, $H = \nabla_\theta^2 \mathcal{L}(\theta^*)$.

    \vspace{0.3cm}
    First-order influence is \emph{linear} in the selected subset:
    \begin{equation}
        f(\theta_\varepsilon) - f(\theta^*) \approx -\varepsilon \, \eqnmarkbox[blue]{i1gf}{g_f^\top} \eqnmarkbox[red]{i1Hg}{H^{-1} g_S}
    \end{equation}
    \annotate[yshift=-1em]{below,left}{i1gf}{\small eval gradient}
    \annotate[yshift=-1em]{below,right}{i1Hg}{\small preconditioned training gradient}

    \vspace{1cm}
    \textbf{Problem:} influence of a group $S = \{z_1, \dots, z_m\}$ is just the \remph{sum of individual influences}~\citep{koh2019accuracy}:
    $$\mathcal{I}(S) = \sum_{i \in S} \mathcal{I}(z_i)$$

    \pause
    \vspace{0.3cm}
    Cannot capture:
    \begin{itemize}
        \item \textbf{Redundancy:} two similar points provide diminishing returns
        \item \textbf{Complementarity:} two points together help more than either alone
        \item \textbf{Interference:} two points together hurt despite each being helpful
    \end{itemize}
\end{frame}

\begin{frame}{Second-Order Group Influence}
    Let $\bar{v} = H^{-1}g_S$, \ $u = H^{-1}g_f$, \ $H_f = \nabla_\theta^2 f(\theta^*)$, \ $H_S = \nabla_\theta^2 \mathcal{L}_S(\theta^*)$.

    \vspace{0.5cm}
    Include the $\varepsilon^2$ term to capture \emph{interactions}:

    \begin{equation}
        f(\theta_\varepsilon) - f(\theta^*) \approx
        \underbrace{-\varepsilon \, g_f^\top \bar{v}}_{\mathcal{I}_1 \text{: first-order}}
        + \frac{\varepsilon^2}{2}\bigg[
        \underbrace{\bar{v}^\top H_f \, \bar{v}}_{\mathcal{T}_1 \text{: interaction attribution}}
        + \underbrace{2\, u^\top H_S \bar{v}}_{\mathcal{T}_2 \text{: curvature correction}}
        \bigg]
    \end{equation}

    \vspace{0.5cm}
    \begin{itemize}
        \item $\mathcal{T}_1$: how the parameter shift interacts with \emph{evaluation} curvature
        \item $\mathcal{T}_2$: how the group's \emph{training} curvature deflects the update
    \end{itemize}
\end{frame}

\begin{frame}{$\mathcal{T}_1$: Pairwise Interaction Expansion}
    Let $v_i = H^{-1}\nabla_\theta \ell(z_i, \theta^*)$. Then $\mathcal{T}_1$ decomposes into pairwise terms:

    \begin{equation}
        \mathcal{T}_1 = \frac{1}{|S|^2}\sum_{i,j \in S} v_i^\top H_f \, v_j
    \end{equation}

    \vspace{0.5cm}
    Each $v_i^\top H_f v_j$ measures how data points $i$ and $j$ \emph{interact} under the evaluation curvature.

    \vspace{0.5cm}
    This enables:
    \begin{itemize}
        \item Detecting \textbf{redundancy}: $v_i^\top H_f v_j > 0$ (diminishing returns)
        \item Detecting \textbf{complementarity}: $v_i^\top H_f v_j < 0$ (synergy)
    \end{itemize}
\end{frame}

\begin{frame}{$\mathcal{T}_2$: Curvature Correction \citep{basu2020second}}
    $$\mathcal{T}_2 = 2\,u^\top H_S \,\bar{v}, \qquad u = H^{-1}g_f, \quad \bar{v} = H^{-1}g_S$$

    \vspace{0.5cm}
    $H_S$ rotates the update direction $\bar{v}$. The correction $u^\top (H_S \bar{v})$ asks: does this rotation \emph{help} or \emph{hurt} evaluation?

    \vspace{0.5cm}
    \begin{itemize}
        \item $\mathcal{T}_2 > 0$: the group's curvature deflects the update \remph{toward} the evaluation objective $\Rightarrow$ \emph{harmful} (increases loss)
        \item $\mathcal{T}_2 < 0$: deflection is \emph{away} from evaluation loss $\Rightarrow$ \emph{beneficial}
        \item $\mathcal{T}_2 \approx 0$: group curvature is orthogonal to evaluation --- no correction needed
    \end{itemize}

    \vspace{0.5cm}
    Large $\mathcal{T}_2$ signals that first-order influence \emph{misjudges} the group's effect.
\end{frame}

\begin{frame}{Group Influence Prediction: Setup}
    \textbf{Task:} predict group influence on similar-data subsets (logit-space nearest neighbors).

    \vspace{0.5cm}
    \begin{center}
    \begin{tabular}{l l l c c}
        \hline
        \textbf{Dataset} & \textbf{Model} & \textbf{Hessian} & $|\mathcal{S}|$ & $n_{\text{subsets}}$ \\
        \hline
        MNIST & LR & Exact & 400 & 50 \\
        MNIST & MLP & EK-FAC & 400 & 50 \\
        FashionMNIST & MLP & EK-FAC & 400 & 50 \\
        Concrete & MLP & EK-FAC & 200 & 50 \\
        Parkinsons & MLP & EK-FAC & 400 & 50 \\
        CIFAR-10 & ResNet-9 & EK-FAC & 400 & 50 \\
        \hline
    \end{tabular}
    \end{center}

    \vspace{0.3cm}
    \textbf{Metric:} AUDS (area under the difference of sorted influence scores) --- higher is better.
\end{frame}

\begin{frame}{Group Influence Prediction: Baselines}
    All baselines are \textbf{additive} (no interaction term).

    \vspace{0.3cm}
    All three predict group influence as an inner product between transformed $g_f$ and $g_S$:
    \begin{itemize}
        \item $\mathcal{I}_1$: \quad $g_f^\top H^{-1} g_S$
        \item \textbf{TRAK} \citep{park2023trak}: \quad $(P g_f)^\top P \, g_S$
        \item \textbf{TracIn} \citep{pruthi2020estimating}: \quad $\sum_c \eta_c \, g_f^{(c)\top} g_S^{(c)}$ \quad --- $g^{(c)}$: gradients at training step $c$
    \end{itemize}
\end{frame}

\begin{frame}{Group Influence Prediction: Results}
    \begin{center}
    \small
    \begin{tabular}{l c c c c c}
        \hline
        & \textbf{TRAK} & \textbf{TracIn} & $\bm{\mathcal{I}_1}$ & $\bm{\mathcal{I}_1 + \mathcal{T}_1}$ & \textbf{Full} \\
        \hline
        MNIST / LR        & 0.47 & 0.43 & 0.55 & 0.60 & \textbf{0.97} \\
        MNIST / MLP       & 0.75 & 0.78 & 0.78 & 0.81 & \textbf{0.87} \\
        FMNIST / MLP      & 0.26 & 0.25 & 0.51 & 0.55 & \textbf{0.86} \\
        Concrete / MLP    & 0.10 & 0.10 & 0.10 & 0.12 & \textbf{0.77} \\
        Parkinsons / MLP  & 0.05 & 0.05 & 0.17 & 0.22 & \textbf{0.56} \\
        CIFAR-10 / ResNet-9 & 0.25 & 0.20 & 0.28 & 0.32 & \textbf{0.51} \\
        \hline
    \end{tabular}
    \end{center}

    \vspace{0.3cm}
    \begin{itemize}
        \item $\mathcal{I}_1 + \mathcal{T}_1$ \emph{consistently} outperforms all additive baselines
        \item Largest gains on MNIST/LR (+0.366 vs Full) and Concrete (+0.672 vs Full)
        \item Gap to Full ($\mathcal{I}_1 + \mathcal{T}_1 + \mathcal{T}_2$) shows $\mathcal{T}_2$ still matters
    \end{itemize}
\end{frame}

\begin{frame}{Application: Selecting Synthetic Data for LLM SFT}
    \textbf{Task:} select complementary synthetic data to add on top of real data.

    \vspace{0.5cm}
    \textbf{Setup:}
    \begin{itemize}
        \item Model: Llama 3.2 3B + LoRA (rank 16)
        \item Pool: MetaMathQA (395K samples: GSM8K + MATH augmented)
        \item Evaluation: GSM8K test (1,319 samples)
    \end{itemize}
\end{frame}

\begin{frame}{Greedy Data Selection}
    Let $w_i = H_f v_i$. Predicted gain when adding point $j$ to current set $\mathcal{S}$:

    \begin{equation}
        \Delta f(j) = \underbrace{-\frac{1}{N} g_f^\top v_j}_{\text{first-order}} + \underbrace{\frac{\alpha}{2N^2}\left(2\sum_{i \in \mathcal{S}} w_j^\top v_i + w_j^\top v_j\right)}_{\text{interaction}}
    \end{equation}

    \vspace{0.5cm}
    \begin{itemize}
        \item Precompute $v_i, w_i$ for all pool data
        \item Each greedy step reduces to dot products
        \item $\alpha = 0$: reduces to first-order top-K
    \end{itemize}
\end{frame}

\begin{frame}{Challenge: Reference Model}
    Influence functions require a reference $\hat{\theta}$ \emph{converged on the training data}.

    \vspace{0.5cm}
    \textbf{Problem:} base LLM is converged on \remph{pretraining data}, not SFT data.

    \pause
    \vspace{0.5cm}
    \textbf{Solution --- Warmup Training:}
    \begin{enumerate}
        \item Fine-tune base model on 5\% random subset $\mathcal{T}$ from pool $\Rightarrow \hat{\theta}_{\mathcal{T}}$
        \item Compute $H$ at $\hat{\theta}_{\mathcal{T}}$
        \item IF predicts: ``which pool data, \textbf{added on top of} $\mathcal{T}$, would help the most?''
        \item Greedy-select $\mathcal{S}$ ($\sim$5\%), final fine-tune on $\mathcal{S} \cup \mathcal{T}$ ($\sim$10\%)
    \end{enumerate}
\end{frame}

\begin{frame}{Results: $\alpha$ Sweep --- Balancing $\mathcal{I}_1$ and $\mathcal{T}_1$}
    \begin{center}
    \begin{tabular}{l c c}
        \hline
        \textbf{Method} & \textbf{GSM\%} & \textbf{Acc} \\
        \hline
        Greedy $\alpha=0.10$ & 64.8\% & 31.87\% \\
        \textcolor{green!50!black}{\textbf{Greedy} $\bm{\alpha=0.25}$} & \textcolor{green!50!black}{64.2\%} & \textcolor{green!50!black}{\textbf{33.08\%}} \\
        Greedy $\alpha=0.50$ & 56.2\% & 32.88\% \\
        Greedy $\alpha=1.00$ & 50.8\% & 32.15\% \\
        \hline
        Random 10\% & 60.9\% & 31.64\% \\
        $\mathcal{I}_1$ top-K & 65.1\% & 31.40\% \\
        \hline
        Full (395K) & 60.8\% & 41.02\% \\
        \hline
    \end{tabular}
    \end{center}

    \vspace{0.3cm}
    \begin{itemize}
        \item Best $\alpha = 0.25$: interaction-aware selection outperforms both random and first-order only
        \item $\alpha = 0$ reduces to $\mathcal{I}_1$ top-K (no interaction awareness)
    \end{itemize}
\end{frame}

\begin{frame}{$\mathcal{T}_1$ as a Diversity Mechanism}
    Higher $\alpha$ (more $\mathcal{T}_1$ weight) $\Rightarrow$ more diverse data source composition:

    \vspace{0.5cm}
    \begin{center}
    \begin{tabular}{l c c}
        \hline
        & \textbf{GSM data} & \textbf{MATH data} \\
        \hline
        $\mathcal{I}_1$ top-K ($\alpha=0$) & 65\% & 35\% \\
        Greedy $\alpha=0.25$ & 64\% & 36\% \\
        Greedy $\alpha=0.50$ & 56\% & 44\% \\
        Greedy $\alpha=1.00$ & 51\% & 49\% \\
        \hline
    \end{tabular}
    \end{center}

    \vspace{0.5cm}
    \begin{itemize}
        \item Without $\mathcal{T}_1$: $\mathcal{I}_1$ concentrates on GSM-type data
        \item With $\mathcal{T}_1$: selects a mix of GSM and MATH data
        \item $\mathcal{T}_1$ selects MATH data that \emph{transfers} to GSM reasoning --- cross-domain complementarity
    \end{itemize}
\end{frame}

\begin{frame}{Summary}
    \begin{itemize}
        \item \textbf{Part 1:} Influence functions approximate LOO retraining
            \begin{itemize}
                \item In deep learning $\to$ PBRF with damping and GNH
            \end{itemize}
        \vspace{0.2cm}
        \item \textbf{Part 2:} Edge influence in GNNs decomposes into parameter shift + message propagation
            \begin{itemize}
                \item Both essential; 0.95 correlation vs.\ 0.1 for prior work
            \end{itemize}
        \vspace{0.2cm}
        \item \textbf{Part 3:} Second-order group influence captures data interactions
            \begin{itemize}
                \item Pairwise interaction term $v_i^\top H_f v_j$ quantifies complementarity/redundancy
                \item Selects diverse, complementary synthetic data for LLM SFT
            \end{itemize}
    \end{itemize}

    \vspace{0.5cm}
    Move beyond i.i.d.\ first-order influence to capture \emph{structure} and \emph{interactions} in training data.
\end{frame}

\begin{frame}[allowframebreaks]{References}
    \printbibliography[heading=none]
\end{frame}

\end{document}